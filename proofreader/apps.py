'''
Proofreader App will enable the Editors to proofread the poetry(items) of the Repository
in a very simple & efficient manner, and get the joy of reading the poetry(items) at the
same time.
'''
from __future__ import unicode_literals

from django.apps import AppConfig


class ProofreaderConfig(AppConfig):
    name = 'proofreader'
